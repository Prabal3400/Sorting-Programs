def radix_sort(a,n):
    m=max(a)
    for pos in range(1,(max/pos)>0,pos*10):
        count_sort(a,n,pos)
def count_sort():
    count=[0]*(k+1)
    for i in range(0,n):
        count([a[i]/pos]%10)+=1
    for i in range(1,k+1):
        count[i]=count[i]+count[i-1]
    for i in range(n-1,-1,-1):
       (count[(a[i]/pos)%10])-=1
       b[(count[(a[i]/pos)%10])]=a[i]
a=[2,1,1,0,2,5,4,0,2,8,7,7,9,2,0,1,9]
n=len(a)
radix_sort(a)
